# dependencies
node_modules/

# OS junk
.DS_Store

# local tooling config (machine-specific paths)
.claude/

# reference copy of the original game (lives in its own repo)
ref/

# local VPS scoreboard state
data/leaderboard.json
data/telemetry.jsonl
data/*.tmp

# Downloaded player-session reports stay local
data/*.jsonl
data/incident*.json
